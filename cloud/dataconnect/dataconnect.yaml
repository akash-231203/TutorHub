specVersion: "v1"
serviceId: "cloud"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "cloud-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
